{
    "builds": [{
        "src": "report_card/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" , "runtime": "python3.9"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "report_card/wsgi.py"
        }
    ]
}